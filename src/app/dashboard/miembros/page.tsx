import { ModulePlaceholder } from '@/shared/components/cyber';

export default function MiembrosPage(): React.ReactElement {
  return (
    <ModulePlaceholder
      title="MIEMBROS"
      description="Directorio y estado de los miembros de la comunidad."
    />
  );
}
