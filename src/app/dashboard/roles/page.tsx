import { ModulePlaceholder } from '@/shared/components/cyber';

export default function RolesPage(): React.ReactElement {
  return (
    <ModulePlaceholder
      title="ROLES"
      description="Jerarquía de roles de Discord sincronizada con la matriz de permisos."
    />
  );
}
