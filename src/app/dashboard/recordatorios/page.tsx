import { ModulePlaceholder } from '@/shared/components/cyber';

export default function RecordatoriosPage(): React.ReactElement {
  return (
    <ModulePlaceholder
      title="RECORDATORIOS"
      description="Agenda táctica y avisos programados para la comunidad."
    />
  );
}
